# OpenARM-VLA Immiation Learning Setup 


```bash
git clone git@github.com:sainavaneet/OpenARM-VLA.git
cd OpenARM-VLA
```

To update the submodules inside the repo, run:

```bash
git submodule update --init --recursive
```




